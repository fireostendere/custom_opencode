// The server entrypoint keeps this discovered plugin package valid. The UI
// implementation is exposed separately through the ./tui export.
export default {
  id: "custom.tui-bundle",
  setup() {},
}

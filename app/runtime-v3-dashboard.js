const $=(id)=>document.getElementById(id)
const state={sessionID:'',telemetry:null,runtime:null,lastTaskID:'',lastBranch:''}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function sid(){const m=/^#\/session\/([^/?]+)/.exec(location.hash||'');return m?decodeURIComponent(m[1]):''}
async function req(path,options={}){const r=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});if(!r.ok)throw new Error(`${r.status} ${(await r.text()).slice(0,240)}`);return r.json()}
function toast(text){const el=$('toast');if(!el)return;el.textContent=text;el.hidden=false;clearTimeout(el._v3);el._v3=setTimeout(()=>el.hidden=true,3500)}

function ensure(){
  const dialog=$('taskCenterDialog');if(!dialog||$('runtimeV3Panel'))return Boolean(dialog)
  const host=document.createElement('details');host.id='runtimeV3Panel';host.className='runtime-v3-panel';host.open=false
  host.innerHTML=`<summary><strong>Runtime control plane</strong><span>AST · RAG · MCP · sandbox · replay · telemetry</span></summary><div id="runtimeV3Stats" class="runtime-v3-stats"></div><div class="runtime-v3-actions"><button type="button" id="runtimeV3Refresh">Refresh</button><button type="button" id="runtimeV3Branch">Branch session</button><button type="button" id="runtimeV3Merge">Merge last branch</button><button type="button" id="runtimeV3WorktreeMerge">Merge worktree</button><button type="button" id="runtimeV3Replay">Replay selected task</button><select id="runtimeV3Sandbox"><option value="safe">safe</option><option value="repo-write" selected>repo-write</option><option value="docker">docker</option><option value="wsl">wsl</option><option value="full-machine">full-machine</option></select><button type="button" id="runtimeV3SetSandbox">Set sandbox</button></div><div class="runtime-v3-search"><input id="runtimeV3Search" placeholder="Semantic repo search…"><button type="button" id="runtimeV3SearchButton">Search</button></div><div id="runtimeV3Results" class="runtime-v3-results"></div>`
  const top=$('runtimeTop');top?.after(host)
  $('runtimeV3Refresh').addEventListener('click',refresh)
  $('runtimeV3Branch').addEventListener('click',branch)
  $('runtimeV3Merge').addEventListener('click',merge)
  $('runtimeV3WorktreeMerge').addEventListener('click',mergeWorktree)
  $('runtimeV3Replay').addEventListener('click',replay)
  $('runtimeV3SetSandbox').addEventListener('click',setSandbox)
  $('runtimeV3SearchButton').addEventListener('click',search)
  $('runtimeV3Search').addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();search()}})
  document.addEventListener('click',event=>{const button=event.target.closest?.('[data-task-detail]');if(button?.dataset.taskDetail)state.lastTaskID=button.dataset.taskDetail},true)
  return true
}
function fmtPct(v){return v==null?'—':`${Math.round(Number(v))}%`}
function render(){if(!ensure())return;const host=$('runtimeV3Stats');if(!host)return;const t=state.telemetry||{},r=t.resources||state.runtime?.resources||{},gpu=r.gpu||{},mcp=t.mcp||{},services=state.runtime?.services||{};const enabled=Object.values(services).filter(Boolean).length,total=Object.keys(services).length;const models=(t.models||[]).filter(m=>m.telemetry?.samples).sort((a,b)=>(b.telemetry?.successRate??-1)-(a.telemetry?.successRate??-1)).slice(0,5);host.innerHTML=`<div class="runtime-v3-card"><span>GPU</span><strong>${fmtPct(gpu.utilization)} busy · ${fmtPct(gpu.vramPercent)} VRAM</strong></div><div class="runtime-v3-card"><span>Router</span><strong>${r.adaptive?'adaptive':'static'} · ${r.pressureHigh?'constrained':'idle'}</strong></div><div class="runtime-v3-card"><span>MCP</span><strong>${mcp.codeMode?'Code Mode':'catalog'} · ${mcp.lazyLoading?'lazy':'eager'}</strong></div><div class="runtime-v3-card"><span>Services</span><strong>${enabled}/${total||0}</strong></div><div class="runtime-v3-wide"><strong>Model telemetry</strong>${models.map(m=>`<div><span>${esc(m.name||m.ref)}</span><code>${m.telemetry.samples} runs · ${m.telemetry.successRate==null?'—':Math.round(m.telemetry.successRate*100)+'%'} · ${m.telemetry.avgLatencyMs==null?'—':Math.round(m.telemetry.avgLatencyMs)+'ms'}</code></div>`).join('')||'<span class="runtime-muted">Telemetry появится после задач.</span>'}</div>`}
async function refresh(){const session=sid();state.sessionID=session;if(!session)return;try{const [runtime,telemetry]=await Promise.all([req(`/client-runtime-v3.json?sessionID=${encodeURIComponent(session)}`),req(`/client-runtime-telemetry.json?sessionID=${encodeURIComponent(session)}`)]);state.runtime=runtime;state.telemetry=telemetry;render()}catch(error){console.debug('runtime v3 dashboard',error)}}
async function branch(){const session=sid();if(!session)return;try{const value=await req('/client-session-branch.json',{method:'POST',body:JSON.stringify({sessionID:session,title:'Runtime branch'})});state.lastBranch=value.session?.id||'';if(state.lastBranch)localStorage.setItem('opencode:last-runtime-branch',state.lastBranch);toast(`Branch: ${state.lastBranch||'created'}`)}catch(error){toast(`Branch: ${error.message}`)}}
async function merge(){const target=sid(),source=state.lastBranch||localStorage.getItem('opencode:last-runtime-branch')||'';if(!target||!source){toast('Нет сохранённой runtime branch');return}try{await req('/client-session-merge.json',{method:'POST',body:JSON.stringify({sourceSessionID:source,targetSessionID:target,includeState:true})});toast('Agent state branch merged')}catch(error){toast(`Merge: ${error.message}`)}}
async function mergeWorktree(){if(!state.lastTaskID){toast('Сначала выбери isolated task');return}try{const value=await req('/client-worktree-merge.json',{method:'POST',body:JSON.stringify({taskID:state.lastTaskID,cleanup:false})});toast(`Worktree merged: ${value.changed?.length||0} paths`)}catch(error){toast(`Worktree merge: ${error.message}`)}}
async function replay(){if(!state.lastTaskID){toast('Сначала открой task details');return}try{const value=await req(`/client-replay.json?taskID=${encodeURIComponent(state.lastTaskID)}`);$('fileTitle').textContent=`Replay ${state.lastTaskID}`;$('fileContent').textContent=JSON.stringify(value,null,2);$('fileDialog').showModal()}catch(error){toast(`Replay: ${error.message}`)}}
async function setSandbox(){if(!state.lastTaskID){toast('Сначала выбери задачу');return}try{await req('/client-task-sandbox.json',{method:'POST',body:JSON.stringify({taskID:state.lastTaskID,sandbox:$('runtimeV3Sandbox').value})});toast(`Sandbox: ${$('runtimeV3Sandbox').value}`)}catch(error){toast(`Sandbox: ${error.message}`)}}
async function search(){const session=sid(),query=$('runtimeV3Search')?.value.trim()||'';if(!session||!query)return;try{const value=await req(`/client-repo-index-v3.json?sessionID=${encodeURIComponent(session)}&query=${encodeURIComponent(query)}`);$('runtimeV3Results').innerHTML=(value.hits||[]).slice(0,20).map(hit=>`<div><strong>${esc(hit.qualified||hit.path)}</strong><span>${esc(hit.type)} · score ${esc(hit.score)}</span></div>`).join('')||'<div class="runtime-muted">Нет совпадений.</div>'}catch(error){toast(`Repo index: ${error.message}`)}}

const observer=new MutationObserver(()=>{if(ensure())render()});observer.observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('hashchange',()=>setTimeout(refresh,100));setInterval(()=>{if($('taskCenterDialog')?.open)refresh()},5000);setTimeout(()=>{ensure();refresh()},500)
